version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: watch
    ports:
      - "5000:5000"
    volumes:
      - "./:/app"
      - "./bin:/app/bin"
      - "./obj:/app/obj"
    environment:
      DOTNET_USE_POLLING_FILE_WATCHER: "1"